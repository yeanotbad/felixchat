self.addEventListener('push',event=>{let data={title:'Felix Chat',body:'You have a new notification',url:'/'};try{data={...data,...event.data.json()}}catch{};event.waitUntil(self.registration.showNotification(data.title,{body:data.body,tag:data.tag||'felix-chat',data:{url:data.url||'/'},icon:'/icon.png',badge:'/icon.png'}));});
self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c)return c.focus()}return clients.openWindow(event.notification.data?.url||'/')}));});
